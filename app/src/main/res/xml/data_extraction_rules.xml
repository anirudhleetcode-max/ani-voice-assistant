<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing Ani stores leaves the device. Conversation transcripts, notification history,
  contact aliases and memory are all excluded from cloud backup and from device-to-device
  transfer, because the user did not consent to that data travelling.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
